<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/11.7.32/sweetalert2.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dashboard.css">
    
    <style>
        /* CSS Tambahan Khusus untuk Halaman User Management */
        .form-container, .table-container {
            background-color: #fff;
            padding: 25px;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
        }

        #addUserForm {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            align-items: end; /* Membuat tombol sejajar dengan input */
        }

        #addUserForm input, #addUserForm select {
            width: 100%;
            padding: 10px 15px;
            border: 1px solid #ddd;
            border-radius: var(--border-radius);
            font-size: 1em;
        }

        #addUserForm button {
            background-color: var(--primary-color);
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-size: 1em;
            transition: var(--transition);
        }

        #addUserForm button:hover {
            opacity: 0.9;
        }

        /* Styling Tabel */
        #userTable {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        #userTable thead th {
            background-color: #f8f9fc;
            padding: 12px 15px;
            text-align: left;
            font-weight: 600;
            color: var(--primary-color);
            border-bottom: 2px solid #eef2f7;
        }

        #userTable tbody td {
            padding: 12px 15px;
            border-bottom: 1px solid #eef2f7;
            color: var(--dark-color);
        }

        #userTable tbody tr:nth-child(even) {
            background-color: #fdfdff;
        }

        .action-buttons-container {
            display: flex;
            flex-direction: row;
            align-items: center;
            gap: 8px;
            width: 100%;
        }

        .btn-edit-user, .btn-delete-user {
            padding: 8px 10px;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-size: 0.9em;
            color: white;
            transition: var(--transition);
            white-space: nowrap;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 36px;
            height: 36px;
        }
        .btn-edit-user i, .btn-delete-user i {
            font-size: 1em;
        }
        .btn-edit-user {
            background-color: #17a2b8;
        }
        .btn-delete-user {
            background-color: var(--danger-color);
        }
        .btn-edit-user:hover, .btn-delete-user:hover {
            opacity: 0.8;
        }
    </style>
</head>
<body>
<div id="userData" data-role="<%= user.role %>" data-line="<%= user.line_name || '' %>" style="display: none;"></div>

<header class="dashboard-header">
    <div class="container">
        <div class="header-content">
            <div class="logo">
                <img src="/image/Logo sanoh 2 2.png" alt="Sanoh Logo" class="logo-img">
                <h1 class="dashboard-title">Andon Dashboard</h1>
            </div>
            <div class="status-info">
                <div class="top-row-info">
                    <div class="user-info">
                        <i class="fas fa-user"></i>
                        <span><%= user.name %></span>
                    </div>
                    <div class="nav-links"> 
                        <% if (user.role === 'admin' || user.role === 'management') { %>
                            <a href="/divisions" class="analytics-link" title="Dashboard Utama">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>Dashboard</span>
                            </a>
                            <a href="/analytics" class="analytics-link" title="Analytics Dashboard">
                                <i class="fas fa-chart-line"></i>
                                <span>Analytics</span>
                            </a>
                            <a href="/plc-monitoring" class="analytics-link" title="PLC Monitoring">
                                <i class="fas fa-network-wired"></i>
                                <span>PLC</span>
                            </a>
                            <a href="/inspect-tables" class="analytics-link" title="Manage Inspect Tables">
                                <i class="fas fa-table"></i>
                                <span>Manage Tables</span>
                            </a>
                        <% } %>
                    </div>
                    <form method="POST" action="/logout" style="display: inline; margin-left: 10px;">
                        <button type="submit" class="logout-btn" title="Logout">
                            <i class="fas fa-sign-out-alt"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</header>

<main class="dashboard-main">
    <div class="container">
        <div class="form-container" style="margin-bottom: 2rem;">
            <h3>Tambah User Baru</h3>
            <form id="addUserForm">
                <input type="text" name="name" placeholder="Nama Lengkap" required>
                <input type="text" name="username" placeholder="Username" required>
                <input type="password" name="password" placeholder="Password (min. 8 karakter)" required>
                <select name="role" id="roleSelect" required>
                    <option value="">Pilih Role</option>
                    <option value="admin">Admin</option>
                    <option value="management">Management</option>
                    <option value="manager">Manager</option>
                    <option value="leader">Leader</option>
                    <option value="maintenance">Maintenance</option>
                    <option value="quality">Quality</option>
                    <option value="engineering">Engineering</option>
                </select>
                <select name="division" id="divisionInput" style="display:none;">
                    <option value="">Pilih Divisi</option>
                    <option value="Brazing">Brazing</option>
                    <option value="Chassis">Chassis</option>
                    <option value="Nylon">Nylon</option>
                </select>
                <select name="line_name" id="lineNameInput" style="display:none;">
                    <option value="">Pilih Line</option>
                    <!-- Options akan diisi oleh JavaScript berdasarkan divisi -->
                </select>
                <button type="submit">Tambah User</button>
            </form>
        </div>
        <div class="table-container">
            <h3>Daftar User</h3>
            <table id="userTable">
                <thead>
                    <tr><th>Nama</th><th>Username</th><th>Role</th><th>Divisi</th><th>Line</th><th>Aksi</th></tr>
                </thead>
                <tbody>
                    <% userList.forEach(u => { %>
                        <tr data-id="<%= u.id %>" data-role="<%= u.role %>" data-division="<%= u.division || '' %>" data-line="<%= u.line_name || '' %>">
                            <td><%= u.name %></td>
                            <td><%= u.username %></td>
                            <td><%= u.role %></td>
                            <td><%= u.division || 'N/A' %></td>
                            <td><%= u.line_name || 'N/A' %></td>
                            <td>
                                <div class="action-buttons-container">
                                    <button class="btn-edit-user" title="Edit">
                                        <i class="fas fa-edit"></i>
                                    </button>
                                    <button class="btn-delete-user" title="Hapus">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</main>

<!-- Edit User Modal -->
<div class="modal" id="editUserModal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Edit User</h3>
            <button type="button" class="modal-close" id="cancelEditUser">×</button>
        </div>
        <div class="modal-body">
            <form id="editUserForm">
                <input type="hidden" name="id" id="editUserId">
                <div class="form-grid-2">
                    <div class="form-group">
                        <label for="editUserName">Nama Lengkap</label>
                        <input type="text" name="name" id="editUserName" placeholder="Nama Lengkap" required>
                    </div>
                    <div class="form-group">
                        <label for="editUserUsername">Username</label>
                        <input type="text" name="username" id="editUserUsername" placeholder="Username" required>
                    </div>
                    <div class="form-group">
                        <label for="editUserPassword">Password</label>
                        <input type="password" name="password" id="editUserPassword" placeholder="Password (kosongkan jika tidak ganti)">
                    </div>
                    <div class="form-group">
                        <label for="editUserRole">Role</label>
                        <select name="role" id="editUserRole" required>
                            <option value="">Pilih Role</option>
                            <option value="admin">Admin</option>
                            <option value="manager">Manager</option>
                            <option value="leader">Leader</option>
                            <option value="maintenance">Maintenance</option>
                            <option value="quality">Quality</option>
                            <option value="engineering">Engineering</option>
                        </select>
                    </div>
                    <div class="form-group" id="editUserDivisionGroup" style="display:none;">
                        <label for="editUserDivision">Divisi</label>
                        <select name="division" id="editUserDivision">
                            <option value="">Pilih Divisi</option>
                            <option value="Brazing">Brazing</option>
                            <option value="Chassis">Chassis</option>
                            <option value="Nylon">Nylon</option>
                        </select>
                    </div>
                    <div class="form-group" id="editUserLineGroup" style="display:none;">
                        <label for="editUserLineName">Line</label>
                        <select name="line_name" id="editUserLineName">
                            <option value="">Pilih Line</option>
                            <!-- Options akan diisi oleh JavaScript berdasarkan divisi -->
                        </select>
                    </div>
                </div>
            </form>
        </div>
        <div class="modal-footer">
            <button type="button" id="cancelEditUserFooter" class="btn btn-secondary">Batal</button>
            <button type="submit" form="editUserForm" class="btn btn-primary">Simpan</button>
        </div>
    </div>
</div>

<script src="/js/users.js"></script>
</body>
</html>
