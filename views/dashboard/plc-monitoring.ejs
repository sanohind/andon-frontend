<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dashboard.css">
    <style>
        .status-box { background: #fff; padding: 2rem; border-radius: 8px; box-shadow: var(--box-shadow); text-align: center; margin-bottom: 2rem;}
        .status-indicator { font-size: 2rem; padding: 1rem 2rem; border-radius: 8px; color: white; display: inline-block; font-weight: bold; }
        .status-indicator.connected { background-color: var(--success-color); }
        .status-indicator.error { background-color: var(--danger-color); }
        .status-indicator.unknown { background-color: #95a5a6; }
        .details-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; }
        .detail-card { background: #fff; padding: 1.5rem; border-radius: 8px; box-shadow: var(--box-shadow); }
        .detail-card h3 { color: var(--primary-color); border-bottom: 2px solid #f0f0f0; padding-bottom: 0.5rem; margin-bottom: 1rem; }
        .detail-card p { font-family: 'Courier New', Courier, monospace; background: #f9fafc; padding: 0.5rem; border-radius: 4px; word-wrap: break-word; }
    </style>
</head>
<body>
     <header class="dashboard-header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <img src="/image/Logo sanoh 2 2.png" alt="Sanoh Logo" class="logo-img">
                    <h1 class="dashboard-title">Andon Dashboard</h1>
                </div>
                <div class="status-info">
                    <div class="top-row-info">
                        <div class="user-info">
                            <i class="fas fa-user"></i>
                            <span><%= user.name %></span>
                        </div>
                        <div class="nav-links"> 
                            <% if (user.role === 'admin') { %>
                                <a href="/" class="analytics-link" title="Dashboard Utama">
                                    <i class="fas fa-tachometer-alt"></i>
                                    <span>Dashboard</span>
                                </a>
                                <a href="/analytics" class="analytics-link" title="Analytics Dashboard">
                                    <i class="fas fa-chart-line"></i>
                                    <span>Analytics</span>
                                </a>
                                <a href="/users" class="analytics-link" title="User Management">
                                    <i class="fas fa-users-cog"></i>
                                    <span>Users</span>
                                </a>
                            <% } %>
                        </div>
                        <form method="POST" action="/logout" style="display: inline; margin-left: 10px;">
                            <button type="submit" class="logout-btn" title="Logout">
                                <i class="fas fa-sign-out-alt"></i>
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </header>
    <main class="dashboard-main">
        <div class="container">
            <div class="page-header">
                <h2>PLC Connection Monitoring</h2>
            </div>

            <div class="details-grid" id="plc-monitoring-grid">
                <p>Loading PLC status...</p>
            </div>
        </div>
    </main>
    
    <script src="/js/plc-monitoring.js"></script>
</body>
</html>