<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/dashboard.css">
    
    <style>
        /* CSS Tambahan Khusus untuk Halaman Ini */
        .form-container, .table-container {
            background-color: #fff;
            padding: 25px;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
        }

        #addTableForm {
            display: flex;
            gap: 15px;
            align-items: center;
        }

        #addTableForm input[type="text"] {
            flex-grow: 1; /* Membuat input mengisi ruang */
            padding: 10px 15px;
            border: 1px solid #ddd;
            border-radius: var(--border-radius);
            font-size: 1em;
        }

        #addTableForm button {
            background-color: var(--primary-color);
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-size: 1em;
            transition: var(--transition);
        }

        #addTableForm button:hover {
            opacity: 0.9;
        }

        /* Styling Tabel */
        #tablesTable {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        #tablesTable thead th {
            background-color: #f8f9fc;
            padding: 12px 15px;
            text-align: left;
            font-weight: 600;
            color: var(--primary-color);
            border-bottom: 2px solid #eef2f7;
        }

        #tablesTable tbody td {
            padding: 12px 15px;
            border-bottom: 1px solid #eef2f7;
            color: var(--dark-color);
        }
        
        .btn-edit, .btn-delete {
            padding: 6px 12px;
            border: none;
            border-radius: var(--border-radius);
            cursor: pointer;
            font-size: 0.9em;
            margin-right: 5px;
            color: white;
            transition: var(--transition);
        }
        .btn-edit { background-color: #17a2b8; } /* Warna biru info */
        .btn-delete { background-color: var(--danger-color); }
        .btn-edit:hover { opacity: 0.8; }
        .btn-delete:hover { opacity: 0.8; }

    </style>
</head>
<body>
<div id="userData" data-role="<%= user.role %>" style="display: none;"></div>

<header class="dashboard-header">
    <div class="container">
        <div class="header-content">
            <div class="logo">
                <img src="/image/Logo sanoh 2 2.png" alt="Sanoh Logo" class="logo-img">
                <h1 class="dashboard-title">Andon Dashboard</h1>
            </div>
            <div class="status-info">
                <div class="top-row-info">
                    <div class="user-info">
                        <i class="fas fa-user"></i>
                        <span><%= user.name %></span>
                    </div>
                    <div class="nav-links"> 
                        <% if (user.role === 'admin') { %>
                            <a href="/" class="analytics-link" title="Dashboard Utama">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>Dashboard</span>
                            </a>
                            <a href="/analytics" class="analytics-link" title="Analytics Dashboard">
                                <i class="fas fa-chart-line"></i>
                                <span>Analytics</span>
                            </a>
                            <a href="/plc-monitoring" class="analytics-link" title="PLC Monitoring">
                                <i class="fas fa-network-wired"></i>
                                <span>PLC</span>
                            </a>
                            <a href="/users" class="analytics-link" title="User Management">
                                <i class="fas fa-users-cog"></i>
                                <span>Users</span>
                            </a>
                        <% } else if (user.role === 'manager') { %>
                            <a href="/" class="analytics-link" title="Dashboard Utama">
                                <i class="fas fa-tachometer-alt"></i>
                                <span>Dashboard</span>
                            </a>
                            <a href="/analytics" class="analytics-link" title="Analytics Dashboard">
                                <i class="fas fa-chart-line"></i>
                                <span>Analytics</span>
                            </a>
                        <% } %>
                    </div>
                    <form method="POST" action="/logout" style="display: inline; margin-left: 10px;">
                        <button type="submit" class="logout-btn" title="Logout">
                            <i class="fas fa-sign-out-alt"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</header>

<main class="dashboard-main">
    <div class="container">
        <div class="form-container" style="margin-bottom: 2rem;">
            <h3>Tambah Meja Inspect Baru</h3>
            <form id="addTableForm">
                <input type="text" name="name" placeholder="Nama Meja (e.g., Meja Inspect 1)" required>
                <select name="division" id="divisionSelect" required>
                    <option value="">Pilih Divisi</option>
                    <option value="Bending">Bending</option>
                    <option value="Chassis">Chassis</option>
                    <option value="Nylon">Nylon</option>
                </select>
                <select name="line_name" id="lineNameSelect" required>
                    <option value="">Pilih Line</option>
                    <!-- Options akan diisi oleh JavaScript berdasarkan divisi -->
                </select>
                <button type="submit">Tambah Meja</button>
            </form>
        </div>
        <div class="table-container">
            <h3>Daftar Meja Inspect</h3>
            <table id="tablesTable">
                <thead>
                    <tr>
                        <th>Nama</th>
                        <th>Divisi</th>
                        <th>Line</th>
                        <th>Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    <% tableList.forEach(table => { %>
                        <tr data-id="<%= table.id %>" data-division="<%= table.division || '' %>" data-line="<%= table.line_name %>">
                            <td><%= table.name %></td>
                            <td><%= table.division || 'N/A' %></td>
                            <td><%= table.line_name %></td>
                            <td>
                                <button class="btn-edit">Edit</button>
                                <button class="btn-delete">Hapus</button>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</main>

<!-- Edit Modal -->
<div class="modal" id="editTableModal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Edit Meja Inspect</h3>
            <button type="button" class="modal-close" id="cancelEditTable">×</button>
        </div>
        <div class="modal-body">
            <form id="editTableForm">
                <input type="hidden" name="id" id="editTableId">
                <div class="form-group">
                    <label for="editTableName">Nama Meja</label>
                    <input type="text" name="name" id="editTableName" required>
                </div>
                <div class="form-group">
                    <label for="editDivision">Divisi</label>
                    <select name="division" id="editDivision" required>
                        <option value="">Pilih Divisi</option>
                        <option value="Bending">Bending</option>
                        <option value="Chassis">Chassis</option>
                        <option value="Nylon">Nylon</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="editLineName">Line</label>
                    <select name="line_name" id="editLineName" required>
                        <option value="">Pilih Line</option>
                        <!-- Options akan diisi oleh JavaScript berdasarkan divisi -->
                    </select>
                </div>
            </form>
        </div>
        <div class="modal-footer">
            <button type="button" id="cancelEditTableFooter" class="btn btn-secondary">Batal</button>
            <button type="submit" form="editTableForm" class="btn btn-primary">Simpan</button>
        </div>
    </div>
</div>

<script src="https://cdnjs.cloudflare.com/ajax/libs/limonte-sweetalert2/11.7.32/sweetalert2.min.js"></script>
<script src="/js/inspect-tables.js"></script>
</body>
</html>